<?xml version="1.0" encoding="UTF-8"?>
<project name="imp.metatooling.common" basedir=".">
    <fail message="Someone imported/invoked the feature's buildCommon.xml!!!"/>

	<import file="../org.eclipse.imp.update/buildCommon.xml"/>

    <!-- Following assumes we're invoking the build from within some plugin project's top-level directory -->
    <xmlproperty file="../org.eclipse.imp.metatooling.feature/feature.xml" prefix="imp.metatooling"/>

    <echo message="Building for IMP Meta-tooling feature version ${imp.metatooling.feature(version)}"/>

    <property name="imp.metatooling.version" value="${imp.metatooling.feature(version)}"/>
</project>
